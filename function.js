

// implicint decoration
function hello(day,time) {
  console.log('Hi it is' + day + 'of the week,and' + time +' o\'clock.');
}

hello('Thursday','10');

//function Expression'  
var hello = function() {
   console.log('Hello, i am a function Expression');
};
 //hello();

  function addition(numOne, numTwo) {
    return numOne + numTwo;
  }

  var nums = addtion(1, 2);

 function area(height, weight) {
  return height + width;
 }

 var userHeight = prompt('What\'s the height');
 var userWidth = prompt('What\'s the width');
  console.log(area(userHeight, userWidth));
  
var area, vaolume, size;

  function getSize(height, width, depth) {
     var area = height = width;
     var vaolume = height = width + depth;
     var size = [area, vaolume];
    return sizes;
  }

 var getArea = getSize(36, 23, 89)[0];
console.log(getArea[0]);
  var getVolume = getSize(36, 23, 89)[1];



function add(a,b) {
  return a + b;
}

